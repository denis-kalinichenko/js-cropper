import Element from "./element";

/**
 * Class representing a Slider
 */
export default class Slider extends Element {
    /**
     * Create a slider.
     */
    constructor() {
        super("input");
    }

    render(parent) {

        super.render(parent);

    }
}